const greet = function (name) {
    return (`Hello, ${name}!`);
}

console.log(greet("Alice")); // Output: Hello, Alice!

//here we are storing the function in the variable greet and then calling it using the variable name. This is called a function expression.